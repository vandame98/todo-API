{
  "compilerOptions": {
    "target": "es2016", // versión de javascript a la que transpilará typescript. ES6 nos permite usar let, const, arrow functions etc.... .
    "module": "commonjs", // Define sistema de importación de módulos, indicando que queremos que use el sistema de módulos de Node que usa commonjs
    "rootDir": "./src", // Indica en qué carpeta estarán los archivos .ts
    "outDir": "./dist", // Indica en qué carpeta se guardará el código transpilado(archivos .js)
    "strict": true, // Activa el modo estricto, lo que mejora la seguridad del código, obligando a ser más explícitos con el tipado
    "noImplicitAny": true, // Nos obliga a definir los tipos correctamente y si nos olvidamos de definir el tipo de una variable, TypeScript nos avisará
    "forceConsistentCasingInFileNames": true, // Evita errores en sistemas de archivos que distinguen entre mayúsculas y minúsculas (por ejemplo, User.ts vs. user.ts).
    "skipLibCheck": true, // Evita que TypeScript analice archivos del tipo .d.ts de bibliotecas externas, lo que acelera la compilación.
    "esModuleInterop": true
  }
}
